// ステージ定義。プレイヤーが必須走路を通過できるよう床上 3 タイルを空ける。
// 行は上から下へ。'.' = 空、'#' = 地面、'P' = プレイヤースポーン、'G' = クロックビーコン、
// 'E' = 巻きネジ障害機 (真下が '#' 必須)、'F' = 時計トンボ (空中配置)、
// 'B' = チクタク爆弾 (真下が '#' 必須)、'C' = 歯車片、'H' = 回復アイテム「予備ゼンマイ」(各ステージ1個)。
// レイアウトの設計: 段差 N=1/2/3 の 3 段差 + 隙間 2 箇所 + ゴール (床上 N=2)。
// ジャンプ高さの理論最大値 ≒ 4 タイル弱なので N=3 まで。

export type TileChar = '.' | '#' | 'P' | 'G' | 'E' | 'F' | 'B' | 'C' | 'H';

/** 敵の種別。'winder'='E'（地上歩行）, 'flyer'='F'（飛行）, 'bomb'='B'（追尾自爆）。 */
export type EnemyType = 'winder' | 'flyer' | 'bomb';

/** 敵の進行方向。-1=左 / 1=右。 */
export type EnemyDir = -1 | 1;

export interface CriticalPathSegment {
  readonly fromCol: number;
  readonly toCol: number;
  readonly supportRow: number;
}

export interface StageDifficultyTarget {
  readonly role: 'intro' | 'intermediate' | 'final';
  readonly minimumScore: number;
}

export interface StageDefinition {
  readonly id: string;
  readonly cols: number;
  readonly rows: number;
  readonly tiles: readonly string[];
  readonly criticalPath: readonly CriticalPathSegment[];
  readonly difficulty: StageDifficultyTarget;
}

export const STAGE_01: StageDefinition = {
  id: 'stage01',
  cols: 120,
  rows: 17,
  tiles: [
    '........................................................................................................................', // row 0
    '........................................................................................................................', // row 1
    '........................................................................................................................', // row 2
    '........................................................................................................................', // row 3
    '........................................................................................................................', // row 4
    '........................................................................................................................', // row 5
    '........................................................................................................................', // row 6
    '........................................................................................................................', // row 7
    '........................................................................................................................', // row 8
    '........................................................................................................................', // row 9
    '........................................................................................................................', // row 10
    '........................................................................................................................', // row 11
    '.................................................................................CC.....................................', // row 12: 高段上面の歯車片 (col 81-82)
    '........................................................CC..............................................................', // row 13: 浮遊歯車片
    '....................................C.............................CC.........................................C..........', // row 14: 必須走路の頭上を開放
    '..P..C.C.C............E......................E..............E.............................E....C......H.......G.........', // row 15: 低天井を除去し、主床ルートを開放（ゴールは床上に設置）
    '##############################...################################....###################################################'  // row 16: 床（隙間 col 30-32, col 65-68）
  ],
  criticalPath: [
    { fromCol: 0, toCol: 29, supportRow: 16 },
    { fromCol: 33, toCol: 64, supportRow: 16 },
    { fromCol: 69, toCol: 119, supportRow: 16 }
  ],
  difficulty: { role: 'intro', minimumScore: 8 }
};
