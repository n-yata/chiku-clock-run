# タスクリスト: スプライトアセット表示とプレイヤー足元補正

| 項目 | 内容 |
|------|------|
| 作成日 | 2026-05-13 |
| 担当 | モドリッチ |
| 関連設計 | `.steering/20260513-fix-sprite-assets-and-player-anchor/design.md` |
| 関連要求 | `.steering/20260513-fix-sprite-assets-and-player-anchor/requirements.md` |

---

## 進め方の原則

- **最小差分で表示不具合を直す**: `BootScene` と `GameScene` に限定して修正する
- 各タスクは [ ] 未着手 / [WIP] 進行中 / [x] 完了 で管理
- ハードコード禁止: 新規 URL、エンドポイント、シークレット、AWS 情報は追加しない
- コミット前は **必ずクルトワ（security-engineer）レビュー**
- 完了報告は出すが、判断が必要な事項が発生したときだけ止めてシャビに確認する

---

## P1: 実装前確認

- [x] **P1-1**: `BootScene.ts` の画像ロードと texture key を確認
  - 完了条件: `TEX_KEY.ground` / `TEX_KEY.goal` / `TEX_KEY.coin` のロード経路が特定できている
- [x] **P1-2**: `GameScene.ts` の地面・コイン生成と `applyPlayerState()` を確認
  - 完了条件: 修正箇所が `buildStage()` / `buildCoins()` / `applyPlayerState()` 周辺に絞れている

---

## P2: フロントエンド実装

### P2-A: アセット読み込み検証

- [x] **P2-A-1**: `BootScene.create()` に必須テクスチャ検証を追加
  - 対象: `TEX_KEY.ground`, `TEX_KEY.goal`, `TEX_KEY.coin`
  - 完了条件: key 不在時に原因 key が分かるエラーになる
- [x] **P2-A-2**: `vite.config.ts` で PNG の data URI 化を防ぐ
  - 完了条件: build 後に地面・ゴール・コインが `dist/assets/*.png` として出力される

### P2-B: プレイヤー足元補正

- [x] **P2-B-1**: `GameScene.applyPlayerState()` で状態変更前の body 下端を保持
  - 完了条件: body が存在する場合に変更前 bottom を取得できる
- [x] **P2-B-2**: 表示サイズ・body サイズ変更後に body 下端を復元
  - 完了条件: `small` / `big` / `fire` の変更後も足元位置が維持される
- [x] **P2-B-3**: 既存 tint / HUD 表示更新処理を維持
  - 完了条件: fire 状態表示と操作説明文の更新が壊れていない

---

## P3: 検証

- [x] **P3-1**: `npm run build` を実行
  - 完了条件: TypeScript / Vite build が成功する
- [x] **P3-2**: ローカル起動で地面タイルを確認
  - 完了条件: 画像として表示され、当たり判定が維持される
- [x] **P3-3**: ローカル起動でコインを確認
  - 完了条件: コイン画像が表示され、取得時に HUD が更新される
- [x] **P3-4**: パワーアップ後の足元を確認
  - 完了条件: `big` / `fire` 変更直後にプレイヤーの足元が床から浮かない

---

## P4: クルトワ（security-engineer）レビュー

- [x] **P4-1**: 変更ファイルすべてのセキュリティレビューをクルトワに依頼
  - URL/エンドポイントのハードコーディング検出
  - シークレット/キーのハードコーディング検出
  - AWS アカウント情報・実 URL・実キーの混入検出
  - XSS、入力処理、CSP、外部通信追加有無の確認
- [x] **P4-2**: 指摘事項を確認
  - Critical / High なし: 完了報告へ進む
  - Critical / High あり: 止めてシャビに確認

---

## P5: ドキュメント更新判断

- [x] **P5-1**: 永続的ドキュメント更新要否を確認
  - 完了条件: 基本設計に影響なしなら更新不要と判断する
- [x] **P5-2**: 必要な場合のみ `docs/` を更新
  - 完了条件: 恒久ルール化が必要な知見だけ反映する

---

## 横断タスク

- [x] **X-1**: 対象ファイルは編集前に現状確認する
- [x] **X-2**: 既存のステージ定義、敵 AI、スコア、ライフ、PWA 設定を変更しない
- [x] **X-3**: 実装中に設計前提が崩れた場合は `decisions.md` を作成して記録する

---

## 進捗マイルストーン

| マイルストーン | 完了条件 |
|--------------|--------|
| **M1: 実装準備完了** | P1 完了 |
| **M2: 修正完了** | P2 完了 |
| **M3: 検証完了** | P3 完了 |
| **M4: レビュー完了** | P4 完了 |
| **M5: スプリント完了** | P5 完了 |

---

## 残る未確定事項

| # | 項目 | トリガ |
|---|------|------|
| Q1 | 画像 URL 生成方式の切り替え | 必須テクスチャ検証追加後も地面・コインが表示されない場合 |
| Q2 | `public/assets/images/` への移行 | Vite import 経路が原因と確認でき、局所修正で解消しない場合 |

---

作成: モドリッチ / 2026-05-13
