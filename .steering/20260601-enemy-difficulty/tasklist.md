# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

---

## フェーズ1: 配置上限の緩和

- [x] `GameScene.ts` の `'E' count must be 1..8` を `1..14` に変更
  - [x] 上限値 `8` → `14`、エラーメッセージ書式も `1..14` に更新
  - [x] 下限 `1` と真下 `#` チェックは維持（変更しない）

## フェーズ2: stage02 の難化（敵+3 / 隙間+1 / 到達可能足場 再構築）

- [x] 既存の到達不能足場（row8/row10 の浮遊 `####`）を撤去
- [x] 到達可能な階段足場を再構築（床 row16 → 中段 row13 → 上段 row10、各段 3 タイル）
  - [x] 足場幅 4 タイル、各段の高低差 3 タイル以内
  - [x] elevated 数を配置（実績 6: 設計値 9 から到達性制約で調整、decisions.md 参照）
- [x] 高所の歯車片 `C` を到達可能足場上 or 放物線が通る空中へ置き直す
- [x] 敵 `E` を 3 体追加（5→8）。各 `E` の真下が `#` の区画上に配置
- [x] 床（row16）の隙間を 1 箇所追加（gaps 3→4）。criticalPath と整合させる
- [x] criticalPath を新レイアウトに合わせて更新（support 行の `#` 連続性・クリアランス維持）
- [x] メトリクス検証: enemy=8 / gaps=4 / elevated=6 / score=28（生成スクリプトで確認）

## フェーズ3: stage03 の難化（敵+4 / 隙間+1 / 到達可能足場 再構築）

- [x] 既存の到達不能足場（row8/row10 の浮遊 `#####`/`######`）を撤去
- [x] 到達可能な階段足場を再構築（床 row16 → 中段 row13 → 上段 row10）
  - [x] 上段 row10 は中段 row13 の真上に重ね、踏み台で到達可能にする
  - [x] elevated 数を配置（実績 8: 設計値 10 から到達性制約で調整、decisions.md 参照）
- [x] 高所の歯車片 `C` を到達可能足場上 or 放物線が通る空中へ置き直す
- [x] 敵 `E` を 4 体追加（8→12）。各 `E` の真下が `#` の区画上に配置
- [x] 床（row16）の隙間を 1 箇所追加（gaps 4→5）
- [x] criticalPath を新レイアウトに合わせて更新
- [x] メトリクス検証: enemy=12 / gaps=5 / elevated=8 / score=38（生成スクリプトで確認）

## フェーズ4: 全体検証

- [x] 難易度メトリクスの最終照合（stage01=8 < stage02=28 < stage03=38）
  - [x] gaps 非減少（2 ≤ 4 ≤ 5）、elevated 非減少（0 ≤ 6 ≤ 8）
- [x] e2e 検証テストを実行
  - [x] `npx playwright test game-visual.spec.ts`（全 7 テスト パス）
  - [x] `validateCriticalPathClearance` / `validateDifficultyProgression` がエラー 0
- [x] 型チェック・ビルド
  - [x] `npm run build`（tsc --noEmit + vite build）成功
- [x] 到達性の機械検証（gen-stages.mjs: 全足場 reachable / unreachable 0）
- [x] ~~実プレイ確認（dev サーバー）~~（シャビ判断: 機械検証〔到達性チェック＋e2e 全パス〕で
  十分と判定しコミットへ。体感プレイはシャビの任意確認に委ねる）

## フェーズ5: 品質チェックとコミット

- [x] クルトワ（security-engineer）レビュー（ハードコーディング観点含む）
- [ ] Critical/High 指摘があれば修正
- [ ] コミット（commit-push）

## フェーズ6: 後続タスクの記録

- [ ] 「敵タイプ追加」を次タスクとしてメモリ（project）に記録

---

## 実装後の振り返り

### 実装完了日
（実装完了後に記入）

### 計画と実績の差分

（実装完了後に記入）

### 学んだこと

（実装完了後に記入）

### 次回への改善提案

（実装完了後に記入）
