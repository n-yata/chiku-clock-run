# プロダクト要件定義書

| 項目 | 内容 |
|------|------|
| 作成日 | 2026-05-04 |
| 最終更新 | 2026-05-27 |
| 担当 | モドリッチ |
| ステータス | 承認済み |

---

## 1. 背景・課題

ブラウザで手軽に遊べる、時計工房を舞台にしたオリジナル 2D 横スクロールアクションゲームを個人プロジェクトとして開発する。
既存のゲームエンジン（Phaser 3）を活用し、インストール不要・URL アクセスだけで即プレイできる形態を実現する。  
GitHub Pages への静的デプロイにより、サーバー維持コストゼロで誰でも遊べる公開ゲームを目指す。  
バックエンドや外部 API を持たないシンプルな構成とし、開発・保守コストを最小化する。

---

## 2. ゴール・目的

| フェーズ | ゴール |
|---------|--------|
| v0.1（MVP） | 1 ステージを横スクロールで踏破できる最小プレイ可能版を GitHub Pages に公開し、デプロイ動線を確立する |
| v0.2 | 巻きネジ障害機・歯車片・収集 HUD を追加し、時計工房を駆け抜けるコア体験を実現する |
| v0.3 以降 | BGM / SE・3 ステージ・タイトル画面・スマホ操作改善・独自時計工房意匠を実装済み |

**成功基準（現行）:** URL を開いたユーザーが説明なしでゲームを開始し、能力取得後も進行不能にならず、段階的に難しくなる 3 ステージで歯車片を集めてクロックビーコンまで到達できる。

---

## 3. スコープ

### 対象範囲

- ブラウザで動作する 2D 横スクロールプラットフォーマー（Phaser 3 ベース）
- プレイヤー操作: キーボード（デスクトップ）+ タッチ操作（横向きスマートフォン）
- ゲーム要素: プレイヤー / 地形 / 巻きネジ障害機 / 歯車片 / クロックビーコン / 収集 HUD / 能力アイテム
- GitHub Pages への静的デプロイ（GitHub Actions による自動ビルド・デプロイ）
- 3 ステージ構成と、クリア時の次ステージ遷移・全クリア後タイトル遷移

### 対象外

- バックエンドサーバー・データベース・外部 API
- ユーザーアカウント・セーブデータ永続化
- ステージセレクト・ワールドマップ
- ポーズ画面
- 新たなライフ / 残機システムの拡張（現行は 3 ライフを実装済み）
- 実行時に取得する外部アセット（画像はローカル生成し、アプリに同梱する）

---

## 4. ステークホルダー

| 役割 | 担当 | 責任範囲 |
|------|------|---------|
| オーナー | シャビ | 要件決定・動作確認・承認 |
| 開発 | モドリッチ（＋チーム） | 設計・実装・ドキュメント整備 |

---

## 5. 機能要件

| # | 機能名 | 概要 | 優先度 |
|---|--------|------|--------|
| F-001 | 横スクロールステージ | 横長ステージをカメラ追従で横スクロール | Must |
| F-002 | プレイヤー操作（キーボード） | ←/→ 移動・Space/↑ ジャンプ・R リスタート | Must |
| F-003 | ゴール判定 | ゴールに触れるとクリア表示・操作無効化 | Must |
| F-004 | 落下リスポーン | 落下閾値を超えるとスポーン位置に戻る | Must |
| F-005 | 巻きネジ障害機 | 壁・段差端で反転して徘徊。踏みつけで停止、横・下接触でミス | Must |
| F-006 | 歯車片取得 | 接触で取得・消滅。取得数を HUD にカウント表示 | Must |
| F-007 | 収集 HUD | 画面左上に「歯車片: X / Y」をカメラ追従なしで常時表示 | Must |
| F-008 | ミス演出・リスタート | ミス時フラッシュ演出 → ステージ完全初期化（障害機・歯車片・プレイヤー復活） | Must |
| F-009 | プレイヤー操作（タッチ） | スマートフォン向けタッチ操作（左スライド移動 / 右タップジャンプ / 能力中は右ダブルタップ発射） | Should |
| F-010 | 横画面専用モバイル表示 | スマートフォンのプレイ対応向きを横画面に限定し、PWA 起動設定も landscape とする。縦画面表示はサポート保証外 | Should |
| F-011 | BGM / SE | Web Audio API による効果音・BGM | Must |
| F-012 | 複数ステージ | 3 ステージと順次遷移・全クリア遷移 | Must |
| F-013 | タイトル画面 | ゲーム開始前と全クリア後のタイトル画面 | Must |
| F-014 | ライフ / 能力アイテム | ぜんまい・パルスコア・クロノクリスタルによる能力変化 | Could |
| F-015 | 能力取得後の通行可能性 | `big` / `fire` 状態でも各ステージの必須ルートを通過できる | Must |
| F-016 | 段階難度マップ | Stage 01 は導入、Stage 02 は中間、Stage 03 は最終難度として危険要素を増加させる | Must |

---

## 6. 非機能要件

### パフォーマンス

- 描画: 60 fps 維持（障害機 5 体 + 歯車片 15 個 + 地形が同時に存在する状況で）
- 初回ロード: GitHub Pages 上で 5 秒以内（モダン回線・キャッシュなし）
- 入力レイテンシ: キー押下 / タッチから自機反応まで 100 ms 以内（体感）
- バンドルサイズ: `dist/assets/*.js` 合計 1.6 MB 以下（gzip 後は 360 KB 以下目安）
- マップ可到達性: 最大サイズ（高さ 84px）のプレイヤーが必須ルート上で通行不能になる箇所を 0 件にする

### セキュリティ

- シークレット管理: バックエンド・外部 API が存在しないため API キー等は不要。`.env` は `.gitignore` 対象として防御済み
- 通信暗号化: GitHub Pages の HTTPS 配信に依存（追加設定不要）
- 認証・認可: なし（公開ゲーム）
- 入力バリデーション: ユーザー入力はキーボードイベント・タッチ座標のみ。Phaser テキストに渡す文字列はリテラル定数か数値型に固定
- CSP: `index.html` の `<meta http-equiv="Content-Security-Policy">` で `default-src 'self'` 系を設定し、外部リソース読み込みを禁止。Phaser Loader の画像処理に必要な `img-src blob:` は許可する

### 可用性

- 動作環境: GitHub Pages（静的ホスティング）。SLO は GitHub サービスレベルに依存
- エラー時フォールバック: 例外発生時も R キー / タッチでリスタート可能な動線を維持
- 無限状態禁止: 落下後に即リスポーン・敵の速度ゼロ事故を起こさない

### 保守性

- ゲーム定数（物理パラメータ・HUD スタイル・テクスチャキー）は `src/config/gameConfig.ts` に集約。シーンファイルへのマジックナンバー直書き禁止
- ステージデータは `src/stages/` に 1 ファイル 1 ステージで配置
- シーン分割（`BootScene` / `TitleScene` / `GameScene`）でアセット準備、開始画面、ゲームロジックを分離

### 拡張性

- ステージ追加: `src/stages/` にファイルを追加するだけで対応可能
- ステージ品質: 必須ルートの最大プレイヤー用クリアランスと難易度の単調増加をステージ契約検証で保証する
- アセット差し替え: `TEX_KEY` で抽象化済み。静的画像は `BootScene` の `load.image()`、動的スプライトは Canvas ビルダーで管理する
- ステージ規模拡大時: `StageDefinition` 型のアダプタを経由して Phaser Tilemap（Tiled エディタ）への移行余地を確保

---

## 7. 制約条件

### 技術的制約

- 対応ブラウザ: 最新 Chrome / Safari / Firefox / Edge
- 対応デバイス: デスクトップ（キーボード）・横向きスマートフォン（タッチ）。縦向きでのレイアウト適応および操作性は保証しない
- 言語・ランタイム: TypeScript 5.4 / Node.js 20（ビルド環境）
- 必須ライブラリ: Phaser 3.80（追加依存は原則禁止）
- ネットワーク前提: インターネット必須（GitHub Pages 配信）。同一オリジン完結・外部 CDN・外部 API 使用禁止

### 予算・期間制約

- インフラコスト: GitHub Pages 無料プランの範囲内
- 期限: 未定（個人プロジェクト）

### 法的・規制制約

- ユーザーの個人情報を収集しないため GDPR・個人情報保護法の対象外
- 出所不明の外部アセットを使用せず、独自の時計工房モチーフ画像と Canvas スプライトをローカル生成して同梱する

---

## 8. 用語定義

| 用語 | 定義 |
|------|------|
| プレイヤー | ユーザーが操作する時計工房の探索者「チク」 |
| タイル | ステージ定義に使う 1 マス単位の文字（`#` 地面 / `P` スポーン / `G` クロックビーコン / `E` 障害機 / `C` 歯車片） |
| HUD | 画面上に常時表示される歯車片取得数等の情報。カメラスクロールに追従しない |
| ゴール | ステージ最終地点のオブジェクト。プレイヤーが触れるとクリア判定 |
| ミス | 敵への横・下接触または落下による失敗。スポーン位置にリセットされる |
| ローカル生成アセット | 時計工房の意匠に合わせてリポジトリ内で再現可能に生成する画像または Canvas スプライト |
| スポーン | プレイヤーの初期出現位置。ミス時はここに戻る |

> 詳細は `docs/glossary.md` を参照。

---

## 9. リスクと対策

| # | リスク | 影響度 | 発生確率 | 対策 |
|---|-------|-------|---------|------|
| R-001 | Phaser 3 の破壊的バージョンアップ | 中 | 低 | `package.json` でバージョンを固定し、アップグレードは計画的に行う |
| R-002 | GitHub Pages の可用性低下 | 中 | 低 | GitHub サービスレベルに依存。代替ホスティング（Netlify / Vercel）への移行余地を確保 |
| R-003 | バンドルサイズ増大によるロード遅延 | 低 | 中 | 追加依存なし方針を徹底。ビルド時のサイズ監視（`npm run build` 出力確認） |
| R-004 | スマホタッチ操作の操作性不足 | 中 | 高 | タッチ操作 UI の継続的改善（v0.3 以降でバーチャルボタン等の導入を検討） |

---

## 10. 未解決事項

| # | 未解決事項 | 決定期限 | 担当 | 備考 |
|---|-----------|---------|------|------|
| TBD-001 | ~~画像アセットの独自意匠化~~ | 2026-05-26 対応済み | シャビ | 歯車片・クロックビーコン・PWA アイコンをローカル生成し、能力物・障害機は Canvas スプライトで実装 |
| TBD-004 | ~~PWA 対応（ホーム画面追加・オフライン起動）~~ | v0.7 対応済み | シャビ | Web App Manifest + Service Worker（Workbox Precache）導入。Android Chrome インストールプロンプト・iOS Safari 手動追加に対応 |
| TBD-002 | ~~BGM / SE の実装方式~~ | 対応済み | バルベルデ | Web Audio API ベースの `AudioManager` で生成・再生 |
| TBD-003 | ~~複数ステージ時のデータ管理方式~~ | 対応済み | バルベルデ | TypeScript のステージ定義配列で 3 ステージを管理 |
